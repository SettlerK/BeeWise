{
  "name": "beewise",
  "version": "0.1.0",
  "description": "BeeWise – digitale Stockkarte: Bienenvölker führen, Durchsichten protokollieren, Aufgaben nach Tracht und Wetter terminieren.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "android:apk": "cap sync android && cd android && ./gradlew assembleDebug",
    "android:bundle": "cap sync android && cd android && ./gradlew bundleRelease"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  }
}
